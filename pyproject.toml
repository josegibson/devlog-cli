[project]
name = "devlog"
version = "0.2.0"
description = "Meta state engine for software projects"
dependencies = [
    "typer[all]",
    "rich",
    "pyyaml",
    "pydantic",
]

[project.scripts]
devlog = "devlog.main:app"

[tool.setuptools]
packages = ["devlog"]
